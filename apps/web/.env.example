# ============================================================================
# Frontend Web App - Environment Configuration Template
# ============================================================================

# Optional custom API Base URL (if hosted on separate domain without Nginx reverse proxy)
# In standard Docker / Nginx setup, leave empty so requests route through relative /api proxy
VITE_API_URL=
