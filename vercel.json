{
  "version": 2,
  "buildCommand": "npm install && npm --prefix apps/api install && npm --prefix apps/web install && npm run build:api && npm run build:web",
  "outputDirectory": "apps/web/dist",
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "/api/index.ts"
    },
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
